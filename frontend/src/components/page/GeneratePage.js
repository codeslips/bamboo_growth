import React from 'react';
import GenerateSpeakPage from './GenerateSpeakPage';

export default function GeneratePage({ onGenerateContent }) {
  return (
    <GenerateSpeakPage onGenerateContent={onGenerateContent} />
  );
}
